# OpenWeatherMap API Key (obtain a free key from https://openweathermap.org/api)
WEATHER_API_KEY=your_api_key_here

# Server Configuration
PORT=5000
HOST=127.0.0.1
FLASK_DEBUG=False

# Allowed CORS Origin (default * allows all origins; restrict to your frontend domain in production)
CORS_ORIGIN=*
